name: build-and-release

on:
  release:
    types: [created]

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Minecraft versions and loaders
        id: mcinfo
        run: |
          if [ "${GITHUB_REF_NAME}" = "1.21.8-polymer" ]; then
            echo "MC_VERSIONS=1.21.6,1.21.7,1.21.8" >> $GITHUB_ENV
            echo "LOADERS=fabric" >> $GITHUB_ENV
          elif [ "${GITHUB_REF_NAME}" = "1.21.9-polymer" ]; then
            echo "MC_VERSIONS=1.21.9,1.21.10,1.21.11" >> $GITHUB_ENV
            echo "LOADERS=fabric,quilt" >> $GITHUB_ENV
          else
            echo "Branch ${GITHUB_REF_NAME} is not allowed for release"
            exit 1
          fi

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle cache
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build
        run: ./gradlew build

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/libs/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to Modrinth
        uses: cloudnode-pro/modrinth-publish@v2
        with:
          token: ${{ secrets.MODRINTH_TOKEN }}
          project: XQQjth2N
          name: ${{ github.event.release.name }}
          version: ${{ github.event.release.tag_name }}
          changelog: ${{ github.event.release.body }}
          loaders: |-
            ${{ env.LOADERS }}
          game-versions: |-
            ${{ env.MC_VERSIONS }}
          files: build/libs/*.jar
          
